{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Static server (python http.server)",
      "runtimeExecutable": "python3",
      "runtimeArgs": ["-c", "import os,functools,http.server,socketserver;P=int(os.environ.get('PORT','8000'));H=functools.partial(http.server.SimpleHTTPRequestHandler,directory='/Users/dida/news-map-deploy');socketserver.TCPServer(('',P),H).serve_forever()"],
      "port": 8000,
      "autoPort": true
    }
  ]
}
